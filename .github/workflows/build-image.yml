name: Docker Image CI

on:
  push:
    branches: [ "*" ]
#  pull_request:
#    branches: [ "main" ]

jobs:
  docker:
    runs-on: ubuntu-latest
    services:
      registry:
        image: registry:2
        ports:
          - 5000:5000
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
        with:
          driver-opts: network=host
      -
        name: Build and push to local registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: localhost:5000/name/app:latest
          cache-from: type=registry,ref=name/app:latest
          cache-to: type=inline
      -
        name: Inspect
        env:
          LOG_LEVEL: ${{ secrets.CA_CERTIFICATE }}
        run: |
          docker run localhost:5000/name/app:latest

#jobs:
#
#  build:
#
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v3
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag aiven:$(date +%s)
